[build]
  base = "frontend"
  publish = "frontend/dist"  # Note: In netlify.toml, publish is sometimes relative to root
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200